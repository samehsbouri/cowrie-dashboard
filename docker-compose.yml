version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - cowrie-log:/cowrie/logs:ro
      - cowrie-var:/cowrie/var/lib/cowrie:ro

volumes:
  cowrie-var:
    external: true
  cowrie-log:
    external: true